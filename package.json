{
  "name": "spoony",
  "version": "1.0.0",
  "private": true,
  "description": "Spoony: planifica el día por la energía que tienes, no por las horas. Carcasa nativa (Capacitor) de index.html.",
  "scripts": {
    "prep": "rm -rf www && mkdir -p www && cp index.html www/index.html",
    "sync": "npm run prep && cap sync",
    "ios": "npm run sync && cap open ios",
    "android": "npm run sync && cap open android"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JHI65/Cucharas.git"
  },
  "license": "UNLICENSED",
  "type": "commonjs",
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.2",
    "@capacitor/share": "^8.0.2"
  }
}
